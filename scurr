a = `LoveFightWrite`;
b = c;

scares = [`sneaks up like a wolf while they're walking in the woods`, `hides under their bed`];
costumes = [`a vampire`, `a ghost`];

c =
{
    power: [Math.floor(Math.random()*100)],
    method: scares[[Math.floor(Math.random()*scares.length)]],
    attire: costumes[[Math.floor(Math.random()*costumes.length)]]
}

if (a.length > 1){c.method};
